import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mandaya.careplus',
  appName: 'Care Plus Mandaya',
  webDir: 'public'
};

export default config;
